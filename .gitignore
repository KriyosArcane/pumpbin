/target
**/target/
/VERSION
/.DS_Store
.claude/
.vscode
# Per-developer MCP server config; contains lab IPs/keys.
/.mcp.json
# Placeholder shellcode files emitted by loader-example build.rs scripts
examples/*/shellcode

# Operator build outputs — don't accidentally commit implants or plugin packs
*.b1n
!examples/starter-plugins/*.b1n
*.elf
*.macho
