name = "uppercase-strings"
description = "Demo: uppercase every ASCII letter in the implant (proves nothing useful: copy this template and replace the body)"
kind = "post-build"
version = "0.1.0"
protocol = 1
platforms = ["any"]
executable = "uppercase_strings.py"
