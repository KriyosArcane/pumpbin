[package]
name = "url-format"
version = "0.1.0"
edition = "2021"
description = "URL formatter module for PumpBin remote mode"

[lib]
crate-type = ["cdylib"]

[dependencies]
pumpbin-plugin-sdk = { path = "../../plugin-sdk" }

[profile.release]
opt-level = "z"
strip = true
lto = true
